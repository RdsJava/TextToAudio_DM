
package org.example;

import jdk.jfr.Enabled;
import org.example.maps.MapCiklo;

import java.io.IOException;
import java.util.Collections;
import java.util.HashMap;
import java.util.Map;

/*
public class CreateForConcanateFile {

    public String[] CreateForConcanateFile (String text) throws IOException {



        CreateText createText = new CreateText();
        ChoiceLanguage choiceLanguage = new ChoiceLanguage();


        if (choiceLanguage.getChoiceLanguage() == 1) {

        }

        String string = createText.createText(text);

        String[] language = new String[string.length()];
        String ss;
        for (
                int i = 0; i < string.length(); i++) {
            ss = map.replace(string.charAt(i));
            language[i] = ss;
        }
            return language;
        }

}
*/